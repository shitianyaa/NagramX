# 2026-08-06 Changelog review

## Requirements

- Inspect all current repository changes.
- Determine suitable changelog contents without modifying the changelog or release notes.

## Checklist

- [x] Inspect repository status, branch, and upstream relationship.
- [x] Inspect committed changes since the relevant release baseline.
- [x] Inspect uncommitted and untracked changes relevant to release notes.
- [x] Compare with the project's existing changelog/release-note format.
- [x] Draft recommended changelog wording.

## Current status

- Review complete; no changelog or release-note file was modified.
- `dev` currently matches `origin/dev`.
- Existing user changes will be preserved.

## Findings

- No local Git tag is available. The review uses commit `6b24fac60` (`update to 12.9.2 (6991)`) as the release baseline because the current project version is `12.9.2 (6992)`.
- The committed release scope contains two main user-facing areas: background video playback with sleep timer/player integration, and app packaging/build identity changes.
- User-facing video changes include preset/custom sleep timers, stop-after-current-video, top mini-player state, playlist/media controls, video title/author/thumbnail fallbacks, and playback-state preservation across full-screen/background transitions.
- The current uncommitted `AudioPlayerAlert.java` change adds opening the selected/current background video in `PhotoViewer` from the cover or playlist row. It has not been built or device-tested in this review.
- The application ID changed from `nu.gpu.nagram` to `com.shitianyaa.nagramx`; release APKs are currently arm64-v8a only. Both are release-note compatibility items.
- CI, signing-secret restoration, release automation, README, and keystore-tracking changes are maintainer-facing and should not dominate the end-user changelog.
- `release-notes/notes.md` is stale/incomplete: it contains only one CI fix and omits the product changes.
- The workflow currently places `ci`/`chore`/`docs` commits under `其他`, so its raw generated output is noisier than a curated end-user changelog.

## Recommended changelog structure

- `新功能`: video sleep timer and background/top-player integration.
- `修复与优化`: state preservation, playlist/media-control stability, title/thumbnail fallbacks, and removal of the unintended round-video floating window.
- `安装说明`: independent package name and arm64-v8a-only APK.
- Include the full-screen reopening bullet only if the current uncommitted `AudioPlayerAlert.java` change ships.

## Commands and checks

- `git status --short --branch`
- `git log --oneline --decorate -20`
- `git log --reverse 6b24fac60..HEAD`
- `git diff --stat` and targeted source/config diffs
- `git diff --check`: passed; only existing LF/CRLF conversion warnings were printed.
- `git diff 6b24fac60..HEAD --check`: passed.

## Files changed by this review

- `Progress/2026-08-06.md`: added the review record only.
- No application source, workflow, README, or release-note content was changed.

## Remaining risks

- No Gradle build, APK installation, emulator test, or device test was run for this changelog review.
- Before release, confirm whether the uncommitted full-screen reopening behavior is included; otherwise remove that changelog bullet.

# 2026-08-06 Release workflow normalization

## Requirements

- Review all current product changes and prepare an accurate release changelog.
- Normalize the Staging, Canary, Release, and pull-request workflows.
- Verify the application ID, Firebase package mapping, version metadata, signing documentation, and APK naming.
- Preserve existing user changes and do not commit or push.

## Checklist

- [x] Audit the current working tree, release baseline, workflows, Gradle packaging, README, and Firebase package mapping.
- [x] Confirm `APP_PACKAGE` and Firebase both use `com.shitianyaa.nagramx`.
- [x] Replace stale package/signing documentation and the remaining old-package runtime check.
- [x] Normalize APK and artifact names.
- [x] Fix workflow triggers, permissions, manual inputs, version checks, and changelog generation.
- [x] Replace the stale generated notes with a curated user-facing changelog.
- [x] Run YAML, shell, Gradle configuration, and diff validation appropriate to the changed files.

## Current status

- Implementation and static/configuration validation complete.
- No commit, push, GitHub Release, or APK build was performed.
- The release keystore is intentionally not present in the checkout, so the current certificate SHA-256 fingerprint cannot be verified locally.

## Files changed

- `.github/workflows/staging.yml`: version/package-triggered Staging build, versioned artifact, prerelease publication, curated/fallback release notes, and APK validation.
- `.github/workflows/canary.yml`: reliable branch trigger, concurrency, versioned Canary artifact, and APK validation.
- `.github/workflows/release.yml`: version/package gate, stable Release publication, stable tag naming, checksum attachment, and least-privilege permissions.
- `.github/workflows/pr.yml`: builds XML/resource changes, removes source rewriting, installs native dependencies, and validates debug artifacts.
- `.github/scripts/generate-changelog.sh`: shared user-facing changelog generator with release-tag/version-commit fallback and internal-commit filtering.
- `.github/scripts/prepare-apk.sh`: validates APK filename, application ID, version, and arm64-only contents, then writes a `.sha256` file.
- `TMessagesProj/build.gradle`: normalizes APK names to `NagramX-<versionName>-<versionCode>-<abi>.apk`.
- `TMessagesProj/src/main/java/tw/nekomimi/nekogram/utils/AndroidUtil.java`: enables release Crashlytics for `com.shitianyaa.nagramx` instead of the retired package.
- `README.md`, `NekoAboutActivity.java`, and `Tools/scripts/upload.py`: replace stale repository/package guidance and document the release contract.
- `release-notes/v12.9.2-6992.md`: curated Chinese user-facing notes for the current release scope.
- `.gitignore`: ignores the generated `release-notes/notes.md` output while preserving versioned curated notes.

## Release decisions

- Stable tag: `v<versionName>-<versionCode>`, for example `v12.9.2-6992`.
- Staging tag: `v<versionName>-<versionCode>-staging`.
- Stable `versionName` is exactly `APP_VERSION_NAME`; Staging and Canary append the seven-character commit ID.
- All current workflows remain `arm64-v8a` only.
- A versioned curated note is preferred; fallback generation excludes `ci`, `chore`, `docs`, `build`, `test`, `style`, and dependency-maintenance commits.
- Application ID and Firebase client package both resolve to `com.shitianyaa.nagramx`.

## Commands and checks

- `gh api .../releases/latest`: confirmed the configured major versions are current for checkout v7, upload-artifact v7, download-artifact v8, setup-java v5, cache v6, Gradle Actions v6, setup-android v4, and ccache-action v1.2.23.
- PyYAML parsed all four workflow files successfully.
- Git Bash `bash -n` passed for both shared scripts.
- Changelog generation test at commit `5bc9d4177` selected baseline `6b24fac60`, emitted only the two user-facing `feat` commits, and excluded CI/chore entries.
- `gradlew.bat :TMessagesProj:tasks --no-daemon --no-configuration-cache`: passed; Gradle loaded all variants and the modified build script without compiling APKs.
- Package mapping check: `APP_PACKAGE` equals the Firebase `package_name`.
- Stale repository/package search: no remaining runtime or documentation references outside the intentional migration note.
- `git diff --check`: passed; only existing LF/CRLF conversion warnings were reported.

## Problems encountered

- Initial Gradle checks failed because the default wrapper cache could not create its lock file and the isolated offline cache lacked the Android Gradle Plugin. After permissions/network were restored, the normal wrapper configuration check passed.
- Direct `bash.exe` invocation did not initialize Git/MSYS utilities; using `D:\Git\bin\bash.exe -lc` resolved the environment and allowed the script tests to run.

## Remaining risks

- No fresh APK was assembled, so `prepare-apk.sh` has passed syntax/source review but has not completed its success path against a newly generated artifact.
- The checkout contains older ignored debug/release APKs named with the previous format and version code `1257`; they were not modified and the new workflow would reject them.
- The current signing certificate fingerprint still needs to be read from the real release keystore or a newly signed APK before publishing a fixed fingerprint in documentation.
- The existing uncommitted `AudioPlayerAlert.java` full-screen reopening behavior is included in the curated notes and must remain in the release scope for those notes to stay accurate.
